spring:
  data:
    mongodb:
      username: hajuna
      password: hajuna
      host: localhost
      port: 27017
      database: customer
      authentication-database: admin

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

server:
  port: 8090